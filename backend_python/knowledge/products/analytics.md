# Database Analytics

**Tagline:** Talk to Your Data. Skip the SQL.

## What It Is

Ask complex questions about your enterprise databases in plain English — just like messaging a colleague. Arvya understands your schema, writes production-ready queries, heals its own errors, and renders stunning interactive charts. All without a single line of SQL from you.

## Supported Databases

PostgreSQL, MySQL, SQLite, Oracle

## Key Stats

- Under 2 minutes database setup and sync
- 0 manual query writing required
- 100% Read-Only Guarantee — your data is never stored, edited, or transmitted

## Core Capabilities

**Natural Language Querying**
Ask anything in plain English — "Show me top customers by revenue last quarter" — and get instant, accurate answers. Arvya translates your question into dialect-aware SQL across all supported databases with no query writing required.

**Interactive Visual Analytics**
Complex analytical questions automatically trigger Python-powered chart synthesis using Pandas and Plotly. Zoom, hover, and map trends in real-time — no Excel, no BI tools, no waiting. Your data becomes a living, interactive dashboard in seconds.

**Self-Healing SQL**
Never see a broken query again. Arvya's built-in 3-retry Self-Healing SQL Critic Agent automatically intercepts syntax errors the moment they occur and fixes them on the fly — so every question gets a clean answer, every time.

**Zero-Config Schema Sync**
Connect your database and Arvya maps it completely — tables, columns, foreign keys, and categorical values — in under two minutes. The AI decodes cryptic column abbreviations into clean human-readable definitions. Smart Sync keeps everything current as your schema evolves.

**Semantic Query Caching**
ChromaDB-powered semantic caching uses vector embeddings to recognise questions asked within 24 hours — even if worded differently — and returns answers instantly, bypassing expensive AI calls entirely.

**100% Read-Only Safety**
Arvya only ever reads your database structure to answer questions. It never stores, edits, alters, or transmits your private data rows. Every query runs through a strictly enforced read-only connection with full audit logging.

## How It Works — 6 Steps

1. Log in to your Arvya workspace
2. Enter your database credentials (host, port, database name)
3. Select the schema to analyse
4. Arvya automatically fetches and syncs the schema in under 2 minutes
5. Ask questions in plain English
6. Get answers and interactive charts instantly

## Getting Started

Contact us at info@arvya.in to connect your database and let your whole team ask questions in plain English.
